package com.example.realestatemanager.edit

/*class EditViewModel: ViewModel(
    val repository: Repository
) {

    private val _data = MutableStateFlow(Property())
    val uiData: StateFlow<Property> = _data

    fun onTypeChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(type = value)
    }

    fun onPriceChanged(value: TextFieldValue) {
        _data.value = _data.value.copy(price = value ?: 0)
    }

    fun onSurfaceChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(surface = value)
    }

    fun onRoomChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(room = value)
    }

    fun onDescritpionChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(description = value)
    }

    fun onAddressChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(address = value)
    }

    fun onInterestChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(interest = value)
    }

    fun onStatusChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(status = value)
    }

    fun onDateOfCreationChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(dateOfCreation = value)
    }

    fun onDateOfSoldChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(dateOfSold = value)
    }

    fun onAgentChanged(value: TextFieldValue) {
        _data.value = uiData.value.copy(agent = value)
    }

}*/